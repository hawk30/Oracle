package hackerarnk;

import static com.sun.org.apache.xalan.internal.xsltc.compiler.util.Type.Node;

/**
 * Created by oracle on 16/10/16.
 */
public class Cycle {
    public static void main(String[] args) {
      /*  boolean hasCycle(Node head) {
            if(head==null)
                return false;
            Node slow=head;
            Node fast=head.next;
            while(fast.next!=null){
                fast=fast.next.next;
                slow=slow.next;
                if(fast==slow)
                    return true;
                else
                    continue;

            }
            return false;
        }*/
        }
    }

